Detexify Backend Server implemented in Haskell
---
REQUIREMENTS
Haskell Platform
    $ cabal install --only-dependencies

COMPILE
    $ cabal build

RUN
    $ dist/build/detexify-hs-backend/detexify-hs-backend
will run the webserver on port 3000

Now it only needs to be trained.
